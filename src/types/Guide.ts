export interface Guide {
    numeroGuia: string;
    origen: string;
    destino: string;
    destinatario: string;
    fecha: string;
    estado: string;
    historial: string[];
}

export type HistoryEntry = string;
